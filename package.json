{
  "name": "claude-ally",
  "version": "0.0.0",
  "private": true,
  "description": "Accessibility-first control layer for Claude Code's CLI decision loop",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "spike": "tsx src/spike/probe.ts",
    "test": "tsx test/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
